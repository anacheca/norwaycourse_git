#norway course git

# renaming te branch to main instead of master
git_default_branch_rename()
git_default_branch_configure(name = "main")

#commit to second branch

#fork

library(usethis)
create_from_github("86fyko/Git_github", fork = TRUE)

#git push on terminal if it does not let me pull
